{
  "name": "ws-control-platform",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev --filter=@wsctl/web --filter=@wsctl/gateway",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.14",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "vitest": "^4.1.7"
  }
}
